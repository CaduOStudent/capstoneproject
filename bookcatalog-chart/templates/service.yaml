apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookcatalog-chart.fullname" . }}
  labels:
    {{- include "bookcatalog-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bookcatalog-chart.selectorLabels" . | nindent 4 }}
