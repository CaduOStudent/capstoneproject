{{- if .Values.ingress.enabled }}
Your application has been deployed and is available via Ingress at:

{{- range .Values.ingress.hosts }}
  http://{{ .host }}
{{- end }}

To access locally, ensure your /etc/hosts contains:

  127.0.0.1 {{ (index .Values.ingress.hosts 0).host }}

Then run:
  kubectl port-forward -n {{ .Release.Namespace }} \
    svc/{{ include "bookcatalog-chart.fullname" . }} 8080:{{ .Values.service.port }}

{{- else }}
Your application has been deployed but Ingress is disabled.
You can port-forward directly:

  kubectl port-forward -n {{ .Release.Namespace }} \
    svc/{{ include "bookcatalog-chart.fullname" . }} 8080:{{ .Values.service.port }}
{{- end }}